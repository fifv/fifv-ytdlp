directories:
    output: dist
appId: ytdlp-gui
productName: YT-dlp GUI
icon: ./build/img/youtube-icon.png
files:
    - build/**
asar: true
asarUnpack:
    - ./build
win:
    target: dir # That's it! Only package, no installer. You can use `zip` to get a portable zip
npmRebuild: false # 這個可以强制他不要rebuild